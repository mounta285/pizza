import { SelectedPizzaDirective } from './selected-pizza.directive';

describe('SelectedPizzaDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedPizzaDirective();
    expect(directive).toBeTruthy();
  });
});
